#ifndef _CHECK_BALANCE_
#define _CHECK_BALANCE_

/*
 * n is the number of +1s
 * there are also n total -1s
*/
void setupPlusAndMinuses(int ary[], int n);
int both_pos_and_neg(int initarray[], int iterations, int n);

#endif